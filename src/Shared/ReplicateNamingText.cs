namespace SkylineNModFilter
{
    internal static class ReplicateNamingText
    {
        public const string EnableLabel = "Number matched replicates for manual ordering";
        public const string RenameLabel = "Use selected column after number prefix";
    }
}
